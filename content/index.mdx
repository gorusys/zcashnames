import { WhereToStart } from "@/components/docs/WhereToStart";

# ZcashNames

ZcashNames maps human-readable names like `alice.zcash` to Zcash shielded addresses. The protocol is called **ZNS** (Zcash Name Service) and lives entirely in the memo field of standard Zcash transactions.

## Where to start

<WhereToStart />
