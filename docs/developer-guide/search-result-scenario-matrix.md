# Search Result Scenario Matrix

| Scenario | Top Row (Name/Status/Price) | Action Row | Footer Row |
|---|---|---|---|
| Available (not for sale) | `Name` + `Available` badge + `Price (ZEC)` inline (price directly after badge) | `Claim Name` (primary) | Feature chips only: `# characters`, `First #`, `No previous owners`, optional `Popular name`; no external links |
| For Sale | `Name` + `For Sale` badge + `Price (ZEC)` inline (price directly after badge) | `Buy Now` (primary) + `Delist from Sale` (secondary), same row | Feature chips first, then `View on CipherScan` and `View on ZcashMe` on the same left-justified footer row |
| Unavailable | `Name` + `Unavailable` badge (do-not-enter icon), no price | `Update Address`, `List for Sale`, `Remove` (all secondary), same row | `View on CipherScan` and `View on ZcashMe`, left-justified; no feature chips |